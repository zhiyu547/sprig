# README 截图说明 / Screenshot notes

Copyright © 2026 zhiyu. These assets are covered by the repository [LICENSE](../../LICENSE).

These are unretouched captures of Sprig 0.4.0 on macOS, taken on 2026-09-17. The app copy uses isolated preferences and has official updates disabled for screenshot work. Its Git interface and operations are the actual released implementation.

All screenshots use a fictional `sprig-demo` repository for a small task-list app. Names, code, commit history, and the commit-message example were prepared for this demonstration. They contain no business repository content or user credentials.

| Asset | Shows |
| --- | --- |
| `workspace.jpg` | Tracked / untracked groups, staged diff, and an illustrative commit message |
| `ai-preview.jpg` | Real outgoing-diff confirmation; the local endpoint and model are illustrative settings |
| `sync-push.jpg` | Real merge / rebase synchronization options; the demo remote is local |
| `history.jpg` | Real commit history and file diff from the synthetic repository |

No AI request was sent while capturing these images. The message shown is a format example, not a claim about a particular model's output. Screenshot timings are incidental UI readings, not performance benchmarks.

截图仅展示演示数据；未调用 AI 服务、未使用真实业务仓库，也未截取第三方产品界面。截图中的提交说明是格式示例，底部耗时不是性能测试结论。
